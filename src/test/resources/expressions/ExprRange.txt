# Author(s):
# 	- Mwexim
# Date:
#	- 2020/12/22
#	- 2023/01/26 (variable support)

test:
	# Number
	set {list::*} to range from 1 to 5
	assert {list::*} = 1, 2, 3, 4 and 5

	# Character
	set {list::*} to range from "d" to "j"
    assert {list::*} = "d", "e", "f", "g", "h", "i" and "j"

    # Variable range
    set {var} to 14
    set {list::*} to range from {var} to 9
    assert {list::*} = 14, 13, 12, 11, 10 and 9
