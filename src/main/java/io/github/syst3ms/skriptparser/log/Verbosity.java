package io.github.syst3ms.skriptparser.log;

/**
 * A verbosity the quantity of non-error messages. Some messages might only be printed if the verbosity is high enough.
 */
enum Verbosity {
    LOW, NORMAL, HIGH, VERY_HIGH, DEBUG
}
