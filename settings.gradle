rootProject.name = 'skript-parser'

new File("$rootDir/src").listFiles().each {
    def projectName = it.name
    include projectName
    project(":${projectName}").projectDir = file("${it.getParentFile().name}/${projectName}")
}